# Nginx configuration snippet for custom 404 page

server {
    listen 80;
    server_name example.com;
    root /var/www/example.com/html;

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;

    location = /404.html {
        internal;
    }
}
